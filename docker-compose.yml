version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: boomy_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - boomy_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: boomy_redis
    ports:
      - "6379:6379"
    networks:
      - boomy_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend API
  backend:
    image: ${DOCKER_USERNAME:-alvaroMarcani}/boomy-backend:latest
    container_name: boomy_backend
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_HTTP_PORTS: 5000
      ConnectionStrings__PostgreSQL: "Host=postgres;Port=5432;Database=boomy_db;Username=boomy_user;Password=Boomy123!@#"
    ports:
      - "5000:5000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - boomy_network

  # Frontend
  frontend:
    image: ${DOCKER_USERNAME:-alvaroMarcani}/boomy-frontend:latest
    container_name: boomy_frontend
    ports:
      - "4200:4200"
    environment:
      ANGULAR_CLI_ANALYTICS: false
    networks:
      - boomy_network

  # pgAdmin - PostgreSQL Administration Tool
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: boomy_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@boomy.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    ports:
      - "5050:80"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - boomy_network
    volumes:
      - pgadmin_data:/var/lib/pgadmin

volumes:
  postgres_data:
  pgadmin_data:

networks:
  boomy_network:
    driver: bridge
